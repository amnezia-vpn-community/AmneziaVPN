name: L2 IKEv2 Integration Tests (disabled)

# Disabled by MISSION §4.2: protocol coverage must exercise the AmneziaVPN
# client entry points, not direct system daemons or helper utilities. Keep this
# workflow inert until an honest-client replacement exists.

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  disabled:
    name: Disabled pending honest-client replacement
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Disabled: MISSION §4.2 requires tests to exercise AmneziaVPN client entry points."
          echo "Do not re-enable this workflow without an honest-client replacement."
