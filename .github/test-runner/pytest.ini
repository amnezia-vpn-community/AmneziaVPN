[pytest]
markers =
    l1: L1 sanity tests — no display required
    l2: L2 smoke tests — requires Xvfb display
    l3: L3 integration tests — requires full VPN stack
    l4: L4 release tests — full matrix, slow
    slow: marks tests as slow
    unit: pure unit tests without external dependencies
timeout = 120
